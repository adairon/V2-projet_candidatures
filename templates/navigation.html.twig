<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">

  {# <a class="navbar-brand" href="{{ path('candidature_index') }}">Mes Candidatures</a> #}

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <ul class="nav nav-tabs">
      <li class="nav-item nav-etape">
        <a href="{{ path('candidature_index') }}" style="color:dodgerblue; font-weight:bold;" class="nav-link bg-light nav-toutes">Toutes ({{ nbCandidatures }})</a>
      </li>
      <li class="nav-item nav-etape">
        <a href="{{ path('candidature_etape', {'id':6}) }}" class="nav-link text-secondary bg-light nav-env">Candidature envoyée</a>
      </li>
      <li class="nav-item nav-etape">
        <a href="{{ path('candidature_etape', {'id':7}) }}" class="nav-link text-primary bg-light nav-aVen">Entretien à venir</a>
      </li>
      <li class="nav-item nav-etape">
        <a href="{{ path('candidature_etape', {'id':8}) }}" class="nav-link text-warning bg-light nav -attRep">Attente réponse</a>
      </li>
      <li class="nav-item nav-etape">
        <a href="{{ path('candidature_etape', {'id':9}) }}" class="nav-link text-success bg-light nav-reten">Candidatures retenues</a>
      </li>
      <li class="nav-item nav-etape">
        <a href="{{ path('candidature_etape', {'id':10}) }}" class="nav-link text-danger bg-light nav-refus">Candidatures réfusées</a>
      </li>
      {# {% for etapes in menu_etapes %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('candidature_etape', {'id':etapes.id}) }}">{{ etapes.etape }}  </a>
        </li>
      {% endfor %} #}
    </ul>
  </div>

    {# <div class="navbar-nav">
      <a class="nav-item nav-link" href="{{ path('candidature_index') }}">Accueil</a>
    </div> #}

{# 
    <div class="nav-item dropdown">

      <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Filtrer par étape
      </a>

      <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
        <a class="dropdown-item" style="color:dodgerblue; font-weight:bold;" href="{{ path('candidature_index') }}">Toutes</a>
          {% for etapes in menu_etapes %}
            <a class="dropdown-item" href="{{ path('candidature_etape', {'id':etapes.id}) }}">{{ etapes.etape }}</a>
          {% endfor %}
      </div>

    </div> #}

    {# <a href="{{ path('candidature_new') }}" class="btn btn-primary new-candidature">Nouvelle candidature</a> #}

{# Bouton de connexion :  #}
{# <a href="" class="btn btn-secondary">Connexion</a> #}

</nav>